---
type: feature
title: Inbox web application for agentic screenshots and files
description: Provide a private-network upload and retrieval service for screenshots and small text/web artifacts used by the agentic environment.
tags:
  - inbox
  - uploads
  - screenshots
  - docker-compose
  - reverse-proxy
  - agentic
sources:
  - title: Agentic Compose stack
    url: https://github.com/TheTeamRAH/docker_compose/tree/main/agentic
  - title: Configuration management repository
    url: https://bitbucket.org/adamhills/configuration_management
status: proposed
---

# Inbox web application for agentic screenshots and files

## Context

Screenshots and small diagnostic files sometimes need to be transferred from a phone into the agentic environment. The service must be independently deployable from the agentic Compose stack and reachable through the existing home-network reverse proxy.

The new public repository is `TheTeamRAH/inbox`. It will be deployed as its own Compose project rather than on `docker-001` or inside the existing agentic stack.

## Goal

Provide a simple mobile-friendly web application that accepts an upload, asks for a descriptive name suffix, stores a timestamped file, and provides links and filtered listings for later retrieval.

## Settled decisions

1. Repository: public GitHub repository `TheTeamRAH/inbox`.
2. Compose project name: `inbox`.
3. Reverse-proxy name: `inbox.rah.home`.
4. The service is a separate Compose stack from the agentic stack.
5. Existing reverse-proxy restrictions are sufficient as the initial access-control boundary; the application does not add a second login mechanism in the first release.
6. No MCP server is included in the first release.
7. Retention is configurable and defaults to 60 days.
8. A user can mark a file as archived; archived files are retained indefinitely.
9. Initial supported file classes are images, `.log`, `.txt`, and `.html` files.

## Scope

- Mobile-friendly upload page.
- Upload flow that accepts a file and a descriptive suffix.
- Timestamped generated filenames that preserve the safe original extension.
- File metadata and a stable download/share link.
- Listing views for all files, today, this week, and this month.
- Search/filter by generated filename or descriptive suffix.
- Archive/unarchive action.
- Configurable retention cleanup with archived files excluded.
- Persistent application metadata and file storage.
- Health endpoint suitable for Compose/reverse-proxy checks.
- Separate Compose stack documentation and configuration-management integration guidance.

## Functional requirements

### Uploads

- The upload form must work from a current mobile browser.
- The user must provide a non-empty descriptive suffix.
- The generated filename must contain a local timestamp and a sanitised suffix, for example `2026-09-19-10-46-12-router-status.png`.
- The original extension must be retained only after validation against the supported file class.
- The application must reject path traversal, executable extensions, unsupported types, and uploads over the configured size limit.
- The application must validate content type from file content where practical, not trust the browser-provided MIME type alone.
- The application must never use an untrusted original filename as a filesystem path.

### Listings and retrieval

- All listings must show filename, size, upload time, archived state, and a download/share link.
- The application must provide today, current-week, and current-month filters using the configured application timezone.
- Download responses must use the stored generated filename and correct content type.
- Image files may be shown with thumbnails or previews; text and HTML files must not be rendered inline in a way that permits stored content to execute in the application origin.
- The listing must remain usable when no files match a filter.

### Retention and archive

- `RETENTION_DAYS` must be configurable and default to `60`.
- A scheduled cleanup mechanism must delete only non-archived files older than the retention period.
- Archived files must not be removed by retention cleanup.
- The archive state must be stored durably with file metadata.
- Cleanup must be idempotent and must not delete a file when metadata and filesystem state disagree without a safe, explicit rule.

## Deployment requirements

- The repository must contain an independent Compose stack with project name `inbox`.
- The stack must not depend on the agentic stack being present or running.
- Persistent host storage paths must be explicit and suitable for configuration-management provisioning.
- The reverse proxy must route `inbox.rah.home` to the web service and preserve the existing restriction policy.
- The stack must not publish a broad or unintended network listener; exposure must follow the existing reverse-proxy pattern.
- No uploaded files, generated database, local environment files, or secrets may be committed to the public repository.
- Agentic retrieval integration must be documented without granting write access to the agentic container.

## Non-goals

- MCP integration.
- Public internet access.
- Multi-user accounts or per-file sharing permissions.
- Cloud object storage.
- Automatic OCR, image analysis, or content classification.
- Editing uploaded files in the browser.
- Indefinite retention for unarchived files.

## Acceptance criteria

- A phone can upload a supported image, log, text, or HTML file through `inbox.rah.home`.
- The upload requires a suffix and produces a timestamped generated filename.
- The newly uploaded file appears in the all-files and appropriate period listing.
- The download/share link retrieves the exact stored file.
- Today, week, and month filters return correct results for the configured timezone.
- A file can be archived and remains present after retention cleanup.
- An unarchived file older than the configured retention period is eligible for cleanup.
- Unsupported files, unsafe names, and oversized files are rejected without creating orphaned metadata.
- The application and storage survive container recreation.
- Compose configuration, application tests, and static deployment validation pass.
- Reverse-proxy integration is documented and can be validated without weakening the existing restriction policy.

## Implementation notes

The implementation may use a small Python web service with SQLite metadata and a host-backed file directory. The storage design must separate the metadata database from the file payload directory so a future agentic read-only mount cannot accidentally expose mutable application state.

Generated names should be unique even when two uploads occur within the same timestamp unit. A stable opaque file ID should be used for application routes; the generated filename is display and download metadata, not an authorization boundary.

The application should expose a health endpoint and keep cleanup separate from request handling. The first implementation should prefer a simple scheduled command or Compose-supported maintenance path over embedding an unbounded background scheduler in the web process.

## Risks and mitigations

- **Public repository:** keep all deployment secrets and local uploaded data outside Git.
- **Reverse-proxy-only security:** retain network restrictions and document that the application has no independent authentication in the first release.
- **Stored HTML:** prevent stored HTML from executing in the application origin; serve it as a download or with a safe content-disposition policy.
- **Disk growth:** configure retention, expose cleanup status, and allow indefinite retention only through explicit archive marking.
- **Metadata/file divergence:** use atomic writes and explicit reconciliation rules.
- **Mobile usability:** test upload and listing flows at phone-sized viewport widths.

## Validation

From the repository root, after implementation:

```bash
uv run pytest
# or the repository's documented isolated test command

docker compose -p inbox config

git diff --check
```

Deployment validation, performed only with separate authorization, must verify:

- the rendered `inbox` Compose project;
- reverse-proxy routing for `inbox.rah.home`;
- upload, listing, download, archive, and cleanup behaviour;
- persistence through container recreation; and
- read-only retrieval from the agentic environment if that integration is enabled.

## Lifecycle

This specification is proposed for review. Implementation, deployment, reverse-proxy changes, and configuration-management changes are not authorized by this specification alone.
